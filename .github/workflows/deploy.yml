name: Deploy Angular to VPS

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Connect & Deploy to VPS
        id: deploy_step
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            echo "📥 Entrando al directorio del proyecto"
            cd ${{ secrets.SERVER_PATH }}

            echo "⬇ Actualizando código"
            git pull origin main

            echo "🔥 Construyendo e instalando sin downtime"
            docker compose up -d --build --remove-orphans

            echo "🧹 Limpiando imágenes antiguas"
            docker system prune -af || true

            echo "✔ Deploy finalizado sin caída del servicio!"

      - name: Notificar éxito por Telegram
        if: success()
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{
              \"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\",
              \"text\": \"🚀 *DEPLOY COMPLETADO* ✔\n\n📌 Proyecto: gestify-solution-client\n🧑‍💻 Autor: ${{ github.actor }}\n📝 Commit: ${{ github.event.head_commit.message }}\n🔢 SHA: ${{ github.sha }}\n📅 Fecha: ${{ github.event.head_commit.timestamp }}\n🔗 https://github.com/${{ github.repository }}/commit/${{ github.sha }}\"
            }" \
            https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage

      - name: Notificar error por Telegram
        if: failure()
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{
              \"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\",
              \"text\": \"❌ *DEPLOY FALLÓ* ❗\n\n🧑‍💻 Autor: ${{ github.actor }}\n📝 Commit: ${{ github.event.head_commit.message }}\n🔢 SHA: ${{ github.sha }}\n📅 Fecha: ${{ github.event.head_commit.timestamp }}\n⚠ Error: El proceso de deploy falló en el servidor.\n🔗 Revisión: https://github.com/${{ github.repository }}/commit/${{ github.sha }}\"
            }" \
            https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage
