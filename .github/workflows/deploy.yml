name: Deploy Angular to VPS

on:
  push:
    branches: [ "main" ]   # Cada push o merge a main = deploy automático

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Connect & Deploy to VPS
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            
            echo "📥 Entrando al directorio del proyecto"
            cd /shared/gestify-solution-client

            echo "⬇ Actualizando código"
            git pull origin main

            echo "🔨 Construyendo nueva imagen Docker"
            docker compose build --no-cache

            echo "🛑 Deteniendo contenedores anteriores"
            docker compose down

            echo "🚀 Levantando aplicación"
            docker compose up -d --remove-orphans

            echo "🧹 Limpiando imágenes antiguas y cache"
            docker system prune -af --volumes

            echo "✔ Deploy finalizado correctamente!"